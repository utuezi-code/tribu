{
  "name": "tribu",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "mobile": "npm run start --workspace=apps/mobile",
    "backend": "npm run start:dev --workspace=apps/backend",
    "lint": "npm run lint --workspace=apps/mobile && npm run lint --workspace=apps/backend",
    "typecheck": "npm run typecheck --workspace=apps/mobile && npm run typecheck --workspace=apps/backend",
    "test": "npm run test --workspace=apps/backend"
  }
}
